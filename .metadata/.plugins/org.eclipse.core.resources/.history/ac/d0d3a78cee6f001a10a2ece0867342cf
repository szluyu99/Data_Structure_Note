package 链表;

public class _21_合并两个有序链表 {
    
	public ListNode mergeTwoLists(ListNode l1, ListNode l2) {
    	ListNode pNode = l1, lNode = l1, rNode = l2;
    	while(l1.next!=null && l2.next!=null){
    		if(l1.val < l2.val){
    			rNode = rNode.next;
    		}
    	}
    }
    
}
